# 增强版配置文件 - 继承 experiment_base.yaml，仅覆盖差异化参数
# 向后兼容：也支持直接作为独立配置加载（此时 base 默认值隐式生效）

defaults:
  - experiment_base

experiment:
  name: "enhanced_attention"
  description: "注意力机制多模态融合实验"
  tags:
    - multimodal
    - diabetes
    - attention
    - fusion

model:
  name: AttentionMultimodal
